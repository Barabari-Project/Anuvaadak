{
    "providers": [
        "...",
        "python",
        "bun"
    ],
    "phases": {
        "setup": "",
        "install": "bun i --no-save && pip install -r requirements.txt --no-cache",
        "build": "cd client && bun run build && cd ..",
        "start": "bun ./server/index.ts"
    }
}